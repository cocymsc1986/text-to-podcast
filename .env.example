ANTHROPIC_API_KEY=
APP_SECRET=
AWS_DEPLOY_ROLE_ARN=
# Optional: API base URL the web UI calls. Leave blank to use the stack's own
# API endpoint; set only to point the UI at a custom domain.
API_BASE_URL=
